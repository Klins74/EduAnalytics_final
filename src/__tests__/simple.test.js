// src/__tests__/simple.test.js

// Простой тест-заглушка, чтобы проверить, что Jest работает.
// Он проверяет, что 2 + 2 действительно равно 4.

test('simple passing test', () => {
  expect(2 + 2).toBe(4);
});